<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helloandroid"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.helloandroid.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 체크박스 페이지 -->
        <activity 
            android:name="com.example.helloandroid.CheckBoxActivity"
            android:label="@string/checkbox">
        </activity>
        
  		<!-- 아답터 뷰 페이지 -->
        <activity 
            android:name="com.example.helloandroid.AdapterViewActivity"
            android:label="@string/adapter_view">
        </activity>
        
        <!-- Grid View 페이지 -->
        <activity 
            android:name="com.example.helloandroid.GridViewActivity"
            android:label="@string/grid_view">
        </activity>
        
        <!-- Gallery View 페이지 -->
        <activity 
            android:name="com.example.helloandroid.GalleryActivity"
            android:label="@string/gallery">
        </activity>
        
    </application>

</manifest>
